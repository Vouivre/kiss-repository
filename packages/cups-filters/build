#!/bin/sh -e

./configure  \
    --prefix=/usr \
    --sysconfdir=/etc \
    --without-php \
    --disable-avahi \
    --disable-dbus \
    --disable-braille \
    --without-rcdir 
	
make
make DESTDIR="$1" install
